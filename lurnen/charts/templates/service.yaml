apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.deployment.name }}-service
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.deployment.port }}
    targetPort: {{ .Values.deployment.targetPort }}
  selector:
    app: {{ .Values.deployment.name }}-service