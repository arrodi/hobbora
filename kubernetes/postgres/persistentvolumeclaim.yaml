apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  storageClassName: STORAGE_CLASS_NAME_VAR
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi